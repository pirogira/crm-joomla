# Что бы улучшили

## Краткосрочно

1. **Интеграционные тесты** — тесты с реальной БД (SQLite/MySQL) для `DatabaseConnector` и полного цикла.
2. **Список компаний** — view со списком компаний с фильтром по стадии и пагинацией.
3. **Обработка ошибок в UI** — показ `TransitionNotAllowedException` пользователю (flash-сообщение) вместо редиректа без обратной связи.
4. **CSRF и валидация** — усилить проверку `task` и `id` в контроллере.

## Среднесрочно

5. **Админ-панель** — управление компаниями, просмотр событий, массовые операции.
6. **API** — REST-эндпоинты для интеграции с внешними системами.
7. **Discovery-формы** — полноценный UI для заполнения и привязки к компании.
8. **Локализация** — завершить все строки в `ru-RU.com_crm.ini` и добавить en-GB.

## Долгосрочно

9. **Расширяемость стадий** — конфигурируемый порядок стадий и правил через XML/БД.
10. **Аналитика** — дашборд по воронке, конверсии между стадиями.
